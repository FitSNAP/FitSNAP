Executable
==========

When running FitSNAP as an executable with

.. code-block:: console

   python -m fitsnap3 input.in

there is a certain sequence of functions that is explained here, and coded in
`fitsnap3/__main__.py`. Specifically, the `main()` function uses the FitSNAP library to execute 
the following sequence of functions that perform a fit.


