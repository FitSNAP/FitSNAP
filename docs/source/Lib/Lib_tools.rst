tools/
======

The :code:`fitsnap3lib/tools` directory contains external code used by FitSNAP which may depend on 
separate external libraries, e.g. PyTorch dataloaders and descriptor data analysis functions.